{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.java) cd /home/vepo/source/engage && mvn -B -q process-sources 2>&1 | tail -20;; esac; } || true",
            "timeout": 60,
            "statusMessage": "Formatting Java (mvn process-sources)..."
          }
        ]
      }
    ]
  }
}
